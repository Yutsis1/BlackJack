package com.example.test

// data class for

data class PlayerData(val id: Int,
                      val GameCount: Int,
                      val name: String,
                      val score: Int,
                      val wins: Int,
                      val loses: Int,
                      val drawns: Int)